---
#tasks file for deploy_docker
 - name: copy Docker Compose files
   become: true
   copy:
    src: files/{{ project_dir }}
    dest: ~/
 - name: Set up .env file
   template:
    src: project-env.j2
    dest: /var/www/.env


# # use files parameter to use multiple docker-compose.yml files
# - name: deploy Docker Compose stack
#   community.docker.docker_compose:
#     project_src: files
#     files:
#     - docker-compose.yml
    # - docker-compose.prod.yml
# - name: Run container
#   become: True
#   shell:
#     cmd: "docker-compose -f docker-compose.yml up -d " #-f docker-compose.prod.yml up -d"
#     # chdir: /mydir/
#  name: run docker-compose
#   docker_compose:
#    project_src: ./files/
#    files:
#    - docker-compose.yml